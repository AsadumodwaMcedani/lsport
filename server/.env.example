NODE_ENV=production
PORT=3000

# Single database (Decision #1)
DB_HOST=localhost
DB_USER=lbbs_user
DB_PASS=CHANGE_ME
DB_NAME=lbbs_prod

# Auth
JWT_SECRET=CHANGE_ME_64_RANDOM_CHARS
ADMIN_EMAIL=lecturer@ufh.ac.za
ADMIN_PASSWORD_HASH=CHANGE_ME_BCRYPT_HASH  # generate: node -e "import('bcrypt').then(b=>b.default.hash(process.argv[1],12).then(console.log))" 'YourPassword'

# Email — DirectAdmin SMTP
SMTP_HOST=mail.lbbs.co.za
SMTP_PORT=587
SMTP_USER=noreply@lbbs.co.za
SMTP_PASS=CHANGE_ME
SMTP_FROM="LS Port — LBBS <noreply@lbbs.co.za>"

# Domains
ADMIN_DOMAIN=work.lbbs.co.za
PORTAL_DOMAIN=portal.lbbs.co.za

# Paths
UPLOAD_PATH=/home/USER/uploads
BACKUP_PATH=/home/USER/backups
